 // With the above scripts loaded, you can call `tippy()` with a CSS
      // selector and a `content` prop:
      tippy('#w-location', {
        content: 'Enter city location. ex: Boston (will work after 3 characters)',
      });